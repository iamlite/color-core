{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "buildCommand": "yarn workspace docs build",
  "installCommand": "yarn workspace color-core build && yarn workspace docs install"
}
